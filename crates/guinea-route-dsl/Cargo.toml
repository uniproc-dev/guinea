[package]
name = "guinea-route-dsl"
description = "The route tree `routes!` declares, parsed - shared by the macro that expands it and by build scripts that generate from it"
version.workspace = true
edition.workspace = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }
winnow = "1.0"

[lints]
workspace = true
